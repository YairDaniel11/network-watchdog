<?xml version="1.0" encoding="utf-8"?>
<!-- LinearLayout רגיל (לא ConstraintLayout) בכוונה - נתמך מ-API 1, ולא
     דורש תלות AndroidX חיצונית שהוסרה מהפרויקט (ראו app/build.gradle). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/colorBgMain"
    android:paddingLeft="20dp"
    android:paddingRight="20dp"
    android:paddingTop="16dp"
    android:paddingBottom="16dp">

    <TextView
        android:id="@+id/headerTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textColor="?attr/colorTextPrimary"
        android:textSize="22sp"
        android:textStyle="bold"
        android:gravity="center"
        android:paddingBottom="4dp" />

    <TextView
        android:id="@+id/rootStatusText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/root_status_checking"
        android:textColor="?attr/colorTextSecondary"
        android:textSize="14sp"
        android:gravity="center"
        android:paddingBottom="20dp" />

    <!-- כפתור רענון ידני -->
    <Button
        android:id="@+id/btnManualRefresh"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:background="@drawable/bg_button_primary"
        android:text="@string/manual_refresh_button"
        android:textColor="@color/text_white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:focusable="true"
        android:focusableInTouchMode="false" />

    <TextView
        android:id="@+id/manualRefreshStatusText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="?attr/colorTextSecondary"
        android:textSize="13sp"
        android:gravity="center"
        android:paddingTop="6dp"
        android:paddingBottom="24dp" />

    <!-- מפריד -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="?attr/colorDivider"
        android:layout_marginBottom="20dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/monitor_explanation"
        android:textColor="?attr/colorTextSecondary"
        android:textSize="12sp"
        android:gravity="center"
        android:paddingBottom="16dp" />

    <!-- כפתור מעקב רשת טלפונית -->
    <Button
        android:id="@+id/btnPhoneMonitor"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_marginBottom="14dp"
        android:background="@drawable/bg_toggle_off"
        android:textColor="@color/text_white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:focusable="true"
        android:focusableInTouchMode="false" />

    <!-- כפתור מעקב אינטרנט -->
    <Button
        android:id="@+id/btnInternetMonitor"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@drawable/bg_toggle_off"
        android:textColor="@color/text_white"
        android:textSize="16sp"
        android:textStyle="bold"
        android:focusable="true"
        android:focusableInTouchMode="false" />

</LinearLayout>
